### Con un ciclo for, imprime la tabla de multiplicar del 7, es decir, 7 × 1, 7 × 2, …, 7 × 10, cada resultado en una línea.

for multiplcacion in range(1, 11):
    resultado = 7 * multiplcacion
    print(f"7 X {multiplcacion} = {resultado}")